/// <reference types="react" />
import { NavConfig, AuthorConfig, ThemeConfig } from "../types";
interface Props extends React.PropsWithChildren {
    nav: NavConfig;
    author: AuthorConfig;
    theme: ThemeConfig;
    tableOfContents: Array<any>;
    showToc: boolean;
    showEditLink: boolean;
    edit_url?: string;
}
export declare const Layout: React.FC<Props>;
export {};
