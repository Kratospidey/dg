/// <reference types="react" />
import { NavLink, NavDropdown, SearchProviderConfig } from "../types";
interface Props {
    author?: string;
    links?: Array<NavLink | NavDropdown>;
    search?: SearchProviderConfig;
}
export declare const NavMobile: React.FC<Props>;
export {};
