interface TocSection {
    id: string;
    title: string;
}
interface TocSectionWithChildren extends TocSection {
    children: Array<TocSection>;
}
export declare function collectHeadings(nodes: NodeListOf<HTMLHeadingElement>): (TocSection | TocSectionWithChildren)[];
export {};
